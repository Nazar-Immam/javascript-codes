"use strict" ; // this treates all js codes as newer version of js

//data types

let name = "Immam" //=>string
let age = 21 // =>integer of range 2^53 , for larger int value use bigint ;
let isLoggedIN = false //=> boolean ;
let email ; //=> undefined ;

/* more data types
    null => this means empty
    undefined => this happens when data type and variable is declared but not initialised.
    symbol => used for uniqueness 
    object => later..
*/

// to know the type of any data use
// typeof

console.log(typeof "Immam");
console.log(typeof 20) ;
